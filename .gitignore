# macOS
.DS_Store
._*

# Editors
.vscode/
.idea/
*.swp

# Node (if a build step is ever added)
node_modules/
npm-debug.log*

# Local env
.env
.env.local

# Internal working docs. The repo root is the published site root, so anything
# committed here is served at beheld.tech/<filename>.
DESIGN-PLAN-*.md
